import Backdrop from "components/Modals/Backdrop";
import { render } from "@testing-library/react";

test("Valida si el componente Backdrop se renderiza correctamente", () => {
  render(<Backdrop />);
});
