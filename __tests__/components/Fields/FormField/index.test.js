import { render } from "@testing-library/react";
import FormField from "components/Fields/FormField";

test("Valida si el componente FormField se renderiza correctamente", async () => {
  render(<FormField />);
});
